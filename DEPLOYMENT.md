# Deployment Guide: Vercel 🚀

Since this is a monorepo, follow these specific steps to host the OWNED dashboard and storefront on Vercel.

## 1. Connect to Vercel

1. Go to [Vercel](https://vercel.com/new).
2. Import the `owned.it.com` repository from your GitHub.

## 2. Project Configuration

In the **Configure Project** screen, set the following:

- **Framework Preset**: `Next.js`
- **Root Directory**: `owned-frontend` 👈 **IMPORTANT**
- **Build Command**: `npm run build` (automatic)
- **Output Directory**: `.next` (automatic)

## 3. Environment Variables

You MUST add the following variables in the Vercel dashboard:

| Key | Value |
|-----|-------|
| `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` | *Your WalletConnect ID* |
| `NEXT_PUBLIC_CONTRACT_ADDRESS` | `0x2CfE077af112B9F6e6Ed39e327D3d31c840401BD` |
| `NEXT_PUBLIC_CHAIN_ID` | `84532` (Base Sepolia) |
| `NEXT_PUBLIC_PINATA_JWT` | *Your Pinata JWT* |

> [!TIP]
> You can find these values in your local `owned-frontend/.env.local` file.

## 4. Deploy

Click **Deploy**. Vercel will build the frontend and provide you with a production URL.

---
*Generated by Antigravity · Owned Protocol 2026*
